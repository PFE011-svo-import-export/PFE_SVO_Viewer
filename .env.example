# Base URL of the AuthService. The viewer calls it directly (no gateway).
VITE_API_URL=http://localhost:5294

# Base URL du LegislationService (FastAPI) : chatbot RAG.
VITE_LEGISLATION_API_URL=http://localhost:8000

# Mapbox access token (https://account.mapbox.com/access-tokens/)..
VITE_MAPBOX_TOKEN=
